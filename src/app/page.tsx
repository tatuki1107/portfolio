import { PortfolioExperience } from "@/components/portfolio-experience";

// Production is deployed from main through the connected Vercel project.
export default function Home() {
  return <PortfolioExperience />;
}
